#include <stdio.h>
/**
 * main - breve description
 * Description: use print hello world
 * Return: void
*/
int main(void)
{
	char l;

	for (l = 122; l >= 97; l--)
		putchar(l);
putchar('\n');
return (0);
}
